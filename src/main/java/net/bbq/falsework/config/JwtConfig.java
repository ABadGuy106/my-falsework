package net.bbq.falsework.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class JwtConfig {
    // JWT 配置将使用 application.properties 中的配置
    // jwt.secret
    // jwt.expiration
    // jwt.refresh-expiration
}